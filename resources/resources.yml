resources:
  jobs:
    metadata_job:
      name: bronze-job
      tasks:
        - task_key: extract_bronze_data
          spark_python_task:
            python_file: ../notebooks/bronze.py
          warehouse_id: ${var.warehouse_id}
          catalog: ${var.catalog}
